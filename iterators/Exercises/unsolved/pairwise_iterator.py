# Exercise 9: Pairwise Iterator
# Create an iterator class called PairwiseIterator that takes an iterable and yields pairs of consecutive elements.

# Example usage:

# numbers = [1, 2, 3, 4, 5]
# pairwise = PairwiseIterator(numbers)
# for pair in pairwise:
#     print(pair)

# Expected output:

# (1, 2)
# (2, 3)
# (3, 4)
# (4, 5)
